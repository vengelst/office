/**
 * UI-Texte für Domäne `common` (DE-Labels der Office-Web-App).
 * Nur String-Konstanten – absichtlich ohne Kommentar je Key.
 */

export const common = {
    loading: 'Wird geladen …',
    empty: 'Keine Daten vorhanden.',
  } as const;
