# Auftrag #14 – Notizen

Umgesetzt: Monteur Typ/Sub (`SubcontractorSelect`, Default EMPLOYED), `SubcontractorContact` + CRUD, `GET /contacts/suggestions`, freie Monteure (`availableOnly` + datumsbasierter Conflict).

Spec lag nicht im Repo vor – aus Auftragstext rekonstruiert und als `claude-arbeitsitems-14-sub-kontakte-freie-monteure.md` abgelegt.
